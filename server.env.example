# Copy to .env (same dir as docker-compose.server.yml) and set strong secrets.
# Redis + MinIO are exposed to the internet for the agents -> DO NOT keep defaults.
POSTGRES_PASSWORD=change-me-pg
REDIS_PASSWORD=change-me-redis-long-random
MINIO_ROOT_USER=iris
MINIO_ROOT_PASSWORD=change-me-minio-long-random
